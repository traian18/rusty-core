[graph]
all-features = true

[advisories]
version = 2
yanked = "deny"
ignore = [
    # paste (proc-macro, transitive via ratatui -> harness/harnessctl TUIs)
    # is unmaintained/archived upstream. No safe upgrade exists per the
    # advisory itself; ratatui would need to drop it or switch to a fork
    # (e.g. `pastey`) for this to close. Not a vulnerability, just
    # abandoned maintenance — accepted until ratatui moves off it.
    "RUSTSEC-2024-0436",
]

[licenses]
version = 2
confidence-threshold = 0.8
allow = [
    "Apache-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    # webpki-roots (Mozilla's embedded CA root bundle, pulled in via
    # reqwest -> hyper-rustls) relicensed its data under CDLA-Permissive-2.0
    # starting with 1.x. Data license, not code, and permissive.
    "CDLA-Permissive-2.0",
    "ISC",
    "MIT",
    "Unicode-3.0",
    "Unicode-DFS-2016",
    "Zlib",
]
exceptions = []

[bans]
multiple-versions = "warn"
wildcards = "allow"
highlight = "all"
workspace-default-features = "allow"
external-default-features = "allow"
skip = []
skip-tree = []

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
