//! Superseded by `backend.rs` (which now owns spawn/parse/session-resume
//! directly, mirroring `harness-integration-codex`'s `backend.rs` shape) and
//! `wire.rs` (pure stream-json parsing). This module is intentionally not
//! declared in `lib.rs` and is kept only as a historical note; delete it in
//! a follow-up cleanup once no branches reference it.
